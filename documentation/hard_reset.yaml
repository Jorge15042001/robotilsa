summary: "Reinicia el dispositivo"
description: "Desenergiza el dispositivo"

produces:
- "application/json"


responses:
  200:
    description: "Success"
    schema:
      type: "object"
      properties:
        success:
          type: "boolean"
          example: true
        str_err:
          type: "string"
          example: 

  400:
    description: "Failure"
    schema:
      type: "object"
      properties:
        success:
          type: "boolean"
          example: false
        str_err:
          type: "string"
          example: "Fallo al reiniciar el dispositivo"

